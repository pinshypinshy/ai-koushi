# AI講師

参考書をアップロードすると、AIが学習に適したステップへ分解し、チャット形式の講義として
解説する Web アプリ。個人利用の MVP。

**設計上の判断はすべて [REQUIREMENTS.md](REQUIREMENTS.md) に記録されている。** 実装で迷ったら
推測せずそちらを読む。決定事項と根拠は §10 に集約されている。README.md は現在の実装状況。

## 構成

| 場所 | 内容 |
|---|---|
| `app/` | React SPA（Vite）。全画面がサーバーと接続済み。通信は `app/src/api.ts` に集約し、`store.ts` の reducer は結果を受け取るだけ（純粋に保つ） |
| `app/src/demo/` | ログイン前のサンプル表示（`/?demo`）。サーバーと同じ形の応答を組み立て、`api.ts` が入口で振り分ける |
| `app/src/help/` | 使い方ガイド（`/` の SC-17、§4.8）の質問候補と回答。静的データで、サーバーへは行かない |
| `app/src/screens/admin/` | 運営管理ページ（`/admin`、§4.7）。読み取り専用。`App.tsx` から lazy で分け、通常利用者へは配信しない |
| `server/` | Hono on Cloudflare Workers。D1・Google OAuth・AI呼び出し層・講義作成 Workflow |
| `shared/api.ts` | app と server が相対パスで共有する API の型。**API の契約はここが唯一の正** |

## コマンド

ルートに `package.json` は無い。`npm install` は必ず `app/` か `server/` の中で実行する。

| コマンド | 場所 |
|---|---|
| `npm run dev` | app（5173）／ server（8787）。両方を並行して動かす |
| `npm run typecheck` | server |
| `npm run build` | app（型チェックを含む） |
| `npm run db:migrate` | server。ローカル D1 へ適用 |
| `npm run allow:add -- <アドレス> "<メモ>"` | server。Google サインインを許可する（本番は `--remote`） |
| `npm run allow:list` | server。許可リストの一覧 |
| `npm run allow:remove -- <アドレス>` | server。許可を取り消す。発行済みセッションは残る |
| `npm run admin:add -- <アドレス>` | server。運営管理ページ（§4.7）の権限を付与。一度サインインした利用者のみ |
| `npm run admin:list` | server。管理者の一覧 |
| `npm run admin:remove -- <アドレス>` | server。権限を取り消す。最後の1人は `--force` が要る |
| `npm run guest:add -- <ID> "<表示名>"` | server。ゲストアカウントを発行（本番は `--remote`） |
| `npm run guest:reset -- <ID>` | server。パスワード再発行。利用者の行は触らないのでデータは残る |

ブラウザからは 5173 だけを使う。`/api` と `/auth` は Vite の proxy が 8787 へ転送する。

## 約束事

- コメントは日本語で書き、根拠となる要件定義書の節番号を添える（例：`§4.1.2`、`SEC-4`）。
  「何をしているか」ではなく「なぜそうしたか」を書く
- 認証は各ルートに `requireUser` を明示する。`/api` にワイルドカードで当てない。
  当てると後から別プレフィックスで生やしたルートまで 401 になる
- AI のトークン計上は AI 呼び出し層の**外側**で行う（`collectUsage` / `flushUsage`）。
  生成ロジックに不具合があっても計上が正しく動く必要があるため（§8.2.4）
- `server/.dev.vars` は読まない。値を書き込まない。秘密情報はユーザー本人が入れる
- 教材はユーザーが持ち込む外部テキストとして扱う。プロンプトに載せる際は必ず
  `wrapMaterial()` を通す（§8.3 SEC-4）
- 受講の応答（③④）は NDJSON で流す。文字の断片（`delta`）だけでなく、保存された発言
  （`done`）と失敗（`error`）を同じ経路で伝える。型は `shared/api.ts` の `LectureStreamLine`
- 確認テストの出題に正解と解説を含めない。「回答する」押下前に答えが判明すると
  §4.3.2 の判定が成立しないため。正誤と解説は採点の応答で初めて返す
- 数式を含みうる文字列は `Markdown` / `InlineMarkdown` を通す。素の文字列として置くと
  `$...$` がそのまま表示される（§4.1.3 は問題文・選択肢・解説すべてを対象とする）
- KaTeX は `rehype-katex` が内部で使う版と揃える（現在 0.16 系）。CSS と組み方が
  ずれると分数や記号が重なって表示される
- `npm install` でパッケージを入れ替えたら開発サーバーを起動し直す。`node_modules` と
  `wrangler.jsonc` は起動時に読み込まれたきりで、自動では反映されない
- サンプル表示の分岐は `api.ts` の入口だけに置く。画面側に「サンプルならこう」を
  書き足さない。段階3でモックを削除したのは、その分岐が全画面に散っていたため
- ゲストのパスワードは平文で扱わない。照合は `auth/password.ts` を通し、発行は
  `npm run guest:add` に任せる。導出の手順を変えるときは
  `server/scripts/guest-lib.mjs` と両方を合わせる（片方だけ変えると照合が壊れる）
- パスワードを忘れたゲストには `npm run guest:reset` を使う。利用者の行を消して作り直すと、
  講義・対話・解答記録が連鎖削除で失われる
- 利用上限は種別で分ける（Google：月8件・$15／ゲスト：月2件・$3）。上限は利用者ごとに
  数えるため、ゲストを増やすと費用がその人数分だけ積み上がる（Q-26）
- 講義の複製（§4.5）は AI を呼ばない。設問まで複写するのは、作り直せば②の費用が二重に
  かかるため
- 月間の講義作成数は `courses` の行数ではなく `ai_usage_logs` の①骨子生成から数える
  （`monthlyCourseCount`）。行数で数えると削除で件数が戻り、「作成→複製→元を削除」で
  上限を回避できる（Q-30）。管理ページの利用者行も同じ SQL に揃える。ずれると画面の
  残量と実際にブロックされる境界が食い違う
- 上限値と月の境界は `server/src/limits.ts` にだけ置く。作成のブロックとサイドバーの
  表示（A-5）は同じ `getUsageSummary()` を見る。二重に定義すると、画面上の残量と
  実際にブロックされる境界がずれる（§8.2.4）
- 利用状況の取り直しは AI を消費した後だけにする（`useUsageRefresh`）。起動時は
  bootstrap に同梱して往復を増やさない（§7.6）。定期ポーリングにはしない
- 使い方ガイド（§4.8）は定型回答のみで AI を呼ばない。回答文に上限件数などの値を写さず、
  画面（サイドバーの利用状況）へ誘導する。写すと `server/src/limits.ts` と二重定義になる
- 運営管理ページの認可は `requireAdmin`（`auth/admin.ts`）を各ルートに明示する。判定は
  毎回 `users.is_admin` を引く。セッションに載せると剥奪しても期限まで管理者のまま残る。
  画面側で管理タブを隠すのは防御にならない（§4.7）
- 管理ページは段階1として読み取りのみ。許可リストの追加・ゲストの発行・上限値と
  システムプロンプトの変更はコマンドとコードに残す。更新系を足すときは Q-27 を読む
- 全利用者を横断して読む SQL は `db/admin.ts` にだけ置く。他のクエリは必ず `user_id` で
  絞る（SEC-2）ため、境界を跨ぐものを1ファイルに閉じ込めて見落としを防ぐ
- コミットメッセージに Co-Authored-By などの共著表記を付けない

## 触る前に読む箇所

| 領域 | 節 |
|---|---|
| 画面・画面遷移 | §3（ワイヤーフレーム）、§4（機能要件） |
| AI 呼び出し | §5（5種類の呼び出しと出力スキーマ） |
| DB・クエリ | §6（D1 向けの型読み替えを含む） |
| 認証・セキュリティ | §4.6（ゲストの発行と総当たり対策を含む）、§8.3 |
| 運営管理ページ | §4.7（権限・記録するもの・段階1の範囲） |
| コストと上限 | §8.2（コンテキストキャッシュは実装必須事項） |

## 現在地

**本番環境（https://ai-koushi.yuki-hiraishi.com）で稼働している。** 講義の作成から受講、
確認テスト、復習までを実データで確認済み。ログイン方法は Google とゲストの2種類で、
ログイン前にはサンプル表示（`/?demo`）がある。開発用のモックデータと画面切替パネルは
削除した。サイドバーには今月の利用状況（A-5）を表示し、80%到達時の警告もここで行う。
`/admin` に運営用の管理ページ（§4.7）があり、利用者・費用・ログイン記録・認証の状態を
読み取り専用で一覧できる。使い方ガイド（§4.8）をサイドバー・講義作成オーバーレイ・空状態から
開ける。段階1は定型回答のみで、AI もサーバーも使わない。講義メニュー（SC-13）から講義を複製でき、
教材とステップ分割を保ったまま対話ログ・解答記録・進捗だけを初期化した講義を作れる（§4.5、Q-30）。

本番と手元の違いで既に踏んだもの（同じ型の不具合は他にも残りうる）：

- Workers は PBKDF2 の反復回数を 100,000 までしか受け付けない。miniflare は制限が無い
- `wrangler d1 execute` は `--remote` と `--file` を同時に指定すると取り込み経路に入り、
  SELECT の行を返さない。読み取りは `--command` を使う
- デプロイ直後に**新しく上げたアセットだけ**が 500 やハングを返すことがある（Cloudflare
  側の一時障害。2026-09-02 に遭遇）。変更の無いファイルは既存の blob を再利用するため
  影響しない。再デプロイしても直らない（同じ内容は再アップロードされない）。本番を
  止めずに確かめるには、`wrangler.jsonc` に一時的に `"preview_urls": true` を足して
  `wrangler triggers deploy`（配信中のバージョンは変わらない）→ `wrangler versions upload`
  → プレビューURLでアセットを検証 → 問題なければ `wrangler versions deploy <id>@100%`。
  壊れた状態で公開してしまったら `wrangler rollback <前のバージョンID>` で即座に戻せる

残っているもの：

| # | 内容 |
|---|---|
| 1 | 初回トークン到達4.1秒（§8.1 の目標は3秒）。ストリーミング自体は動作している |
| 2 | 本番の③講義本文が64.4秒（ローカルは6.1秒）。ストリーム完了までを含む値で、受け手の読み取り速度にも左右されるため未切り分け |
| 3 | 確認テストだけ失敗した講義の再生成。サーバーの受け口はあるが画面に導線が無い |
| 4 | 生成失敗（SC-06）の画面を意図的に再現する手段が無い |
