/**
 * ログイン前に中身を見てもらうためのサンプル講義（「画面を見る」）。
 *
 * 実データと同じ型で扱えるよう、サーバーが返す表現へ demo/index.ts で組み替える。
 * ここは素材だけを持ち、正解と解説も保持する（採点の応答で使う）。
 */

export interface DemoStep {
  orderIndex: number
  title: string
  objective: string
  keyPoints: string[]
  /** AI の発話。1要素が1発話にあたる */
  script: string[]
}

export interface DemoChoice {
  body: string
  isCorrect: boolean
}

export interface DemoQuestion {
  stem: string
  explanation: string
  choices: DemoChoice[]
  /** 関連するステップの id。複数持つものが横断設問（§4.3.1.1） */
  coveredStepIds: string[]
}

function step(
  orderIndex: number,
  title: string,
  objective: string,
  keyPoints: string[],
  script: string[],
): DemoStep {
  return { orderIndex, title, objective, keyPoints, script }
}

function q(
  stem: string,
  explanation: string,
  choices: [string, boolean][],
  coveredStepIds: string[],
): DemoQuestion {
  return {
    stem,
    explanation,
    choices: choices.map(([body, isCorrect]) => ({ body, isCorrect })),
    coveredStepIds,
  }
}

/** ステップ id は題名から作る。設問との対応付けを読みやすくするため */
export const stepId = (title: string) => `demo-step-${title}`
const S = stepId

export const DEMO_MATERIAL = `# Git入門

バージョン管理システム Git の基礎を扱う。

## 基本コマンド

作業ディレクトリを Git の管理下に置くには \`git init\` を使う。

| コマンド | 役割 |
|---|---|
| \`git add\` | 変更をステージに載せる |
| \`git commit\` | ステージの内容を履歴に記録する |
| \`git status\` | 現在の状態を確認する |

## ブランチ

ブランチは履歴の分岐である。作業を本流から切り離して進められる。

\`\`\`bash
git switch -c feature/login
\`\`\`

## コンフリクト

同じ行を複数のブランチで編集した状態でマージすると衝突が起きる。

衝突の発生確率は、変更行数を $n$、共有ファイル数を $m$ とすると概ね次に比例する。

$$
P_{\\mathrm{conflict}} \\propto \\frac{n \\times m}{T}
$$

## リモート

\`git fetch\` はリモートの変更を取得するだけで、作業ツリーには反映しない。

一方 \`git pull\` は \`git fetch\` と \`git merge\` を続けて実行する。作業ツリーが書き換わるのは
後半の merge であり、この merge は当然コンフリクトを起こしうる。

### 主なリモート操作

- \`git remote -v\` : 登録されているリモートを一覧する
- \`git push origin main\` : ローカルの main をリモートへ送る
- \`git fetch origin\` : リモートの更新を取得する（作業ツリーは変わらない）
- \`git pull origin main\` : 取得したうえで現在のブランチへ統合する

> リモートへの push が拒否される場合、多くはリモート側に自分が持っていない
> コミットが存在している。まず取得して統合してから、あらためて push する。
`

export const DEMO_STEPS: DemoStep[] = [
  step(
    1,
    '基本コマンド',
    'リポジトリを作成し、変更を記録できるようになる',
    ['git init', 'git add とステージ', 'git commit', 'git status / git log'],
    [
      'ステップ1「基本コマンド」から始めましょう。\n\nGit で最初につかんでおきたいのは、変更が **3つの場所**を移動していく、という点です。\n\n📌 変更が通る3つの場所\n- 作業ツリー：いま編集しているファイル\n- ステージ：次のコミットに入れる変更の待機場所\n- リポジトリ：履歴として残った状態\n\nここさえ押さえると、ほとんどのコマンドが「どこからどこへ動かすものか」で説明できてしまいます。\n\nここまでで引っかかるところはありますか？',
      'では、具体的なコマンドを見ていきましょう。\n\n`git init` は、いまいるディレクトリを Git の管理下に置くコマンドです。`.git` という隠しディレクトリが作られて、履歴はここに溜まっていきます。\n\n`git add <file>` は作業ツリーの変更をステージへ移します。そして `git commit` が記録するのは、ステージに載っているものだけです。**編集しただけで add していないファイルは、commit しても履歴に入りません。**\n\nこの「add を挟む」やり方、なんのためにあると思いますか？',
      'いい着眼点ですね。理由は、**1回のコミットに入れる変更を自分で選べる**ことにあります。\n\nたとえば5つのファイルを編集していても、関係する2つだけを add してコミットすれば、履歴が意味のあるまとまりで残ります。バグ修正と機能追加が1つのコミットに混ざってしまうと、あとから片方だけ取り消すことができません。\n\nいまの状態を見るなら `git status`、これまでの履歴を見るなら `git log` を使います。\n\nステップ1はここまでです。分からないところはありますか？',
    ],
  ),
  step(
    2,
    'ブランチ',
    'ブランチを作成・切り替え・統合できるようになる',
    ['ブランチの実体', 'git switch', 'git merge', 'fast-forward'],
    [
      'ステップ2「ブランチ」に進みましょう。\n\nブランチは「履歴上のある地点を指す名前」です。フォルダのコピーではありません。`git switch -c feature/login` とすると、いまのコミットを指す新しい名前が作られて、そこへ移動します。\n\n実体が名前だけなので、ブランチを作るのは一瞬で終わります。ここは他のバージョン管理システムと大きく違うところですね。\n\nイメージはつかめましたか？',
      '作ったブランチを本流に取り込むのが `git merge` です。\n\n分かれてから本流側が動いていない場合、Git は名前の位置を進めるだけで済ませます。これを **fast-forward** と呼びます。新しいコミットは作られません。\n\n一方、どちらにも変更があるときは、2つの履歴を束ねる**マージコミット**が作られます。\n\n⚠️ どちらになるかは、分岐したあとに本流が動いたかどうかだけで決まります。\n\nここまで大丈夫でしょうか？',
      'ステップ2の要点をまとめますね。\n\n📝 まとめ\n- ブランチはコミットを指す名前であって、コピーではない\n- `git switch -c` で作成と移動を同時にできる\n- マージには fast-forward とマージコミットの2種類がある\n\n次のステップでは、このマージがうまくいかない場合を扱います。',
    ],
  ),
  step(
    3,
    'コンフリクト',
    '衝突の発生条件を理解し、解決できるようになる',
    ['衝突が起きる条件', 'コンフリクトマーカー', '解決手順'],
    [
      'ステップ3「コンフリクト」です。\n\nマージが自動で終わらないことがあります。**同じファイルの同じ行を、両方のブランチで違う内容に書き換えていた場合**ですね。Git はどちらが正しいか判断できないので、そこだけ人間に委ねてきます。\n\n逆に言うと、同じファイルでも直した場所が離れていれば、Git は自動でマージしてくれます。「同じファイルを触ったら衝突する」わけではない、というのは押さえておきたいところです。',
      'コンフリクトが起きると、ファイルにこんなマーカーが挿入されます。\n\n```\n<<<<<<< HEAD\n現在のブランチの内容\n=======\nマージしようとしたブランチの内容\n>>>>>>> feature/login\n```\n\n直し方は3ステップです。\n\n1. マーカーで囲まれた箇所を、正しい内容に書き換える\n2. マーカーの行そのものを削除する\n3. `git add` して `git commit` する\n\n⚠️ 2番を忘れて、マーカーごとコミットしてしまう事故がよくあります。',
      'ステップ3はここまでです。\n\n衝突は「失敗」ではなく、Git が判断を人間に引き渡した状態だと捉えてください。慌てて `--abort` する前に、まず何と何がぶつかっているのかを読むのが大事です。',
    ],
  ),
  step(
    4,
    'リモート',
    'リモートリポジトリと変更をやり取りできるようになる',
    ['origin', 'git push', 'git fetch と git pull の違い'],
    [
      '最後のステップ「リモート」です。\n\nリモートリポジトリは、ネットワーク越しに共有される Git リポジトリのことです。名前は慣習的に `origin` が使われます。\n\n`git push` はローカルのコミットをリモートへ送り、`git fetch` はリモートの変更を取ってきます。\n\n⚠️ ここで大事なのが、**fetch は作業ツリーを変えない**という点です。取ってくるだけで、手元のファイルには反映されません。',
      '`git pull` は、`git fetch` と `git merge` をまとめて実行するコマンドです。\n\nつまり pull は「取ってきて、さらにいまのブランチへ統合する」ところまでやります。作業ツリーが変わるのは、この merge の部分ですね。\n\n**そしてこの merge は、当然コンフリクトを起こしえます。** ステップ3で扱った内容が、ここで効いてきます。',
      'これで全ステップが完了しました。\n\n基本コマンドの3層構造から始めて、ブランチでの分岐、それを統合するときに起きるコンフリクト、そしてリモートとのやり取りまで、ひととおり扱いましたね。\n\n確認テストで、理解度を測ってみてください。',
    ],
  ),
]

export const DEMO_QUESTIONS: DemoQuestion[] = [
  q(
    'git add と git commit の関係として正しいものはどれか。',
    'git add は作業ツリーの変更をステージへ移す操作で、履歴に記録するのは git commit です。add を挟む設計により、1回のコミットに含める変更を選べます。',
    [
      ['add はステージに載せ、commit がステージの内容を履歴に記録する', true],
      ['commit がステージに載せ、add が履歴に記録する', false],
      ['add と commit はどちらも履歴に記録し、記録先が異なる', false],
      ['add はリモートへ送り、commit はローカルへ記録する', false],
    ],
    [S('基本コマンド')],
  ),
  q(
    '5つのファイルを編集した状態で、そのうち2つだけを git add して git commit した。履歴に記録されるのはどれか。',
    'commit が記録するのはステージの内容だけです。add していない3ファイルの変更は作業ツリーに残り、履歴には入りません。',
    [
      ['add した2ファイルの変更のみ', true],
      ['編集した5ファイルすべての変更', false],
      ['変更は記録されず、コミットが失敗する', false],
      ['最後に編集した1ファイルの変更のみ', false],
    ],
    [S('基本コマンド')],
  ),
  q(
    'git init を実行したときに作られるものはどれか。',
    'git init は履歴を保存する .git ディレクトリを作成します。最初のコミットは作られないため、この時点で git log を実行してもエラーになります。',
    [
      ['履歴を保存する .git ディレクトリ', true],
      ['最初のコミットと .git ディレクトリ', false],
      ['リモートリポジトリへの接続設定', false],
      ['main ブランチと最初のコミット', false],
    ],
    [S('基本コマンド')],
  ),
  q(
    'Git におけるブランチの実体として正しいものはどれか。',
    'ブランチは特定のコミットを指す名前にすぎません。作業ディレクトリのコピーを持たないため、作成のコストがほぼゼロになります。',
    [
      ['あるコミットを指す名前', true],
      ['作業ディレクトリ全体のコピー', false],
      ['変更差分を集めたファイル', false],
      ['リモートに置かれた履歴の複製', false],
    ],
    [S('ブランチ')],
  ),
  q(
    'ブランチを切ってから本流側に一度も変更が加えられていない状態でマージした。何が起きるか。',
    '分岐後に本流が動いていない場合、Git は名前の位置を進めるだけで済ませます。これが fast-forward で、マージコミットは作られません。',
    [
      ['fast-forward となり、マージコミットは作られない', true],
      ['必ずマージコミットが作られる', false],
      ['変更がないためマージが拒否される', false],
      ['コンフリクトが発生する', false],
    ],
    [S('ブランチ')],
  ),
  q(
    'コンフリクトが発生する条件として正しいものはどれか。',
    '同じファイルを触っただけでは衝突しません。編集箇所が離れていれば Git は自動でマージします。衝突するのは同じ行が異なる内容に編集された場合です。',
    [
      ['同じファイルの同じ行が、両方のブランチで異なる内容に編集されたとき', true],
      ['同じファイルを両方のブランチで編集したとき', false],
      ['両方のブランチでファイル数が異なるとき', false],
      ['マージ元のブランチが本流より古いとき', false],
    ],
    [S('コンフリクト')],
  ),
  q(
    'コンフリクトを解決する手順として正しいものはどれか。',
    'マーカー行を削除し忘れてコミットする事故が頻発します。内容の修正とマーカーの削除は別の作業だと意識してください。',
    [
      ['内容を正しく書き換え、マーカー行を削除し、add して commit する', true],
      ['内容を正しく書き換えれば、マーカー行は自動で削除される', false],
      ['git merge --abort を実行してから再度マージする', false],
      ['衝突したファイルを削除してからコミットする', false],
    ],
    [S('コンフリクト')],
  ),
  q(
    'git fetch を実行した直後の作業ツリーの状態として正しいものはどれか。',
    'fetch はリモートの変更を取得するだけで、作業ツリーには反映しません。反映するには merge が別途必要です。',
    [
      ['取得しただけで、作業ツリーは変化していない', true],
      ['リモートの内容が作業ツリーに反映されている', false],
      ['作業ツリーがリモートの状態で上書きされている', false],
      ['ローカルのコミットがリモートへ送信されている', false],
    ],
    [S('リモート')],
  ),
  // ---- 以下は横断設問（複数ステップを組み合わせないと解けない。§5.3 R-8）----
  q(
    'ブランチ A で編集した行を、main でも同じ行だけ別の内容に編集していた。この状態で main に A をマージすると何が起きるか。',
    'ブランチの統合（ステップ2）とコンフリクトの発生条件（ステップ3）の両方を理解していないと解けません。両方に変更があるため fast-forward にはならず、かつ同じ行のため自動マージもできません。',
    [
      ['fast-forward にはならず、コンフリクトが発生する', true],
      ['fast-forward となり、A の内容で上書きされる', false],
      ['マージコミットが自動で作られ、衝突は起きない', false],
      ['main の内容が優先され、A の変更は破棄される', false],
    ],
    [S('ブランチ'), S('コンフリクト')],
  ),
  q(
    'git pull を実行したところコンフリクトが発生した。この現象の説明として正しいものはどれか。',
    'pull が fetch と merge の合成であること（ステップ4）と、merge が衝突しうること（ステップ3）を結び付ける必要があります。衝突しているのは pull ではなく、その内部の merge です。',
    [
      ['pull は内部で merge を行うため、merge と同じ条件で衝突しうる', true],
      ['pull は fetch のみを行うため、本来コンフリクトは起きない', false],
      ['リモートとの通信エラーがコンフリクトとして報告されている', false],
      ['ローカルに未コミットの変更がある場合にのみ起きる現象である', false],
    ],
    [S('コンフリクト'), S('リモート')],
  ),
  q(
    'ファイルを編集したがステージには載せていない。この状態でブランチを切り替えると、その変更はどうなるか。',
    '3層構造（ステップ1）とブランチが名前にすぎないこと（ステップ2）の理解が必要です。未ステージの変更は作業ツリーにあり、ブランチの切り替えでは失われません。',
    [
      ['作業ツリーに残ったまま、切り替え先へ持ち越される', true],
      ['切り替え時に破棄される', false],
      ['自動的にコミットされてから切り替わる', false],
      ['元のブランチに退避され、戻るまで参照できなくなる', false],
    ],
    [S('基本コマンド'), S('ブランチ')],
  ),
  q(
    'リモートへ push したところ拒否された。ローカルには未 add の編集があり、リモートには自分が持っていないコミットがある。まず行うべき対応はどれか。',
    '基本コマンド・リモート・コンフリクトの3ステップにまたがる設問です。push が拒否されるのはリモートに未取得のコミットがあるためで、未 add の編集は push の対象ですらありません。',
    [
      ['リモートの変更を取得して統合してから、あらためて push する', true],
      ['未 add の編集を add すれば push が通るようになる', false],
      ['--force を付けて push し、リモートを上書きする', false],
      ['新しいブランチを作り直してから push する', false],
    ],
    [S('基本コマンド'), S('リモート')],
  ),
]
